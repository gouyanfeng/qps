# 首页工作台 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将现有欢迎页改造为“当前销售的 CRM 工作台 + 轻量经营看板”。

**Architecture:** 后端新增 `GET /api/admin/dashboard/crm` 聚合接口，按当前登录用户统计药材基地、沟通记录和图表数据。前端新增 dashboard API 模块和首页展示组件，首页页面负责取数、loading、错误态、跳转和布局，组件只负责展示。

**Tech Stack:** .NET 8、ASP.NET Core、MediatR、Entity Framework Core、SQL Server、Vue 3、TypeScript、Element Plus、ECharts、vue-echarts、Playwright。

## Global Constraints

- 所有文档文件名使用中文。
- 不要把中间产物放在代码仓库里。
- 临时测试文件、草稿、分析结果、构建输出、实验脚本或调试产物统一放到 `E:\Code\QPS\CodexTemp`。
- 新增前端和后端相关测试文件放到 `E:\Code\QPS\CodexTemp`，不要直接放进代码仓库。
- 默认数据范围按当前登录用户过滤，使用登录态 `userId` 和 CRM 药材基地 `ownerUserId`。
- 第一版不做“全部/负责人切换”、复杂地图、销售排行榜、同比环比分析和大屏模式。
- 图表使用现有依赖 `echarts` 和 `vue-echarts`，不新增图表库。
- 首页接口不能清理或重置 CRM 业务数据。

---

## File Structure

后端新增文件：

- `E:\Code\QPS\QPS-HT\src\2.QPS.Application\Contracts\Dashboard\CrmDashboardDto.cs`：定义首页聚合响应 DTO。
- `E:\Code\QPS\QPS-HT\src\2.QPS.Application\Features\Dashboard\GetCrmDashboardQuery.cs`：实现首页聚合查询和统计逻辑。
- `E:\Code\QPS\QPS-HT\src\4.QPS.WebAPI\Controllers\Admin\Dashboard\DashboardController.cs`：暴露 `GET /api/admin/dashboard/crm`。

后端修改文件：

- `E:\Code\QPS\QPS-HT\src\2.QPS.Application\Interfaces\IDbContext.cs`：确认已暴露 `CrmHerbBases`、`CrmFollowRecords`、`CrmBusinessEntityAttributes`，如已存在不改。
- `E:\Code\QPS\QPS-HT\src\4.QPS.WebAPI\Program.cs`：不新增注册，MediatR 已扫描 `QPS.Application`。

前端新增文件：

- `E:\Code\QPS\QPS_WEB_ADMIN\src\api\modules\dashboard.ts`：封装首页聚合接口。
- `E:\Code\QPS\QPS_WEB_ADMIN\src\views\home\components\HomeMetricCards.vue`：顶部 KPI。
- `E:\Code\QPS\QPS_WEB_ADMIN\src\views\home\components\TodayFollowTable.vue`：今日待跟进表格。
- `E:\Code\QPS\QPS_WEB_ADMIN\src\views\home\components\HomeQuickActions.vue`：快捷操作。
- `E:\Code\QPS\QPS_WEB_ADMIN\src\views\home\components\RecentFollowRecords.vue`：最近沟通。
- `E:\Code\QPS\QPS_WEB_ADMIN\src\views\home\components\FollowFunnelChart.vue`：跟进漏斗。
- `E:\Code\QPS\QPS_WEB_ADMIN\src\views\home\components\MainProductDistributionChart.vue`：主营品类分布。
- `E:\Code\QPS\QPS_WEB_ADMIN\src\views\home\components\FollowTrendChart.vue`：近 7 天跟进趋势。

前端修改文件：

- `E:\Code\QPS\QPS_WEB_ADMIN\src\views\home\index.vue`：替换欢迎页为工作台页面。

临时测试文件：

- `E:\Code\QPS\CodexTemp\tests\QPS.UnitTests\Dashboard\CrmDashboardQueryTests.cs`：后端聚合逻辑临时测试。
- `E:\Code\QPS\CodexTemp\tests\QPS_WEB_ADMIN\home-dashboard.spec.ts`：前端首页 Playwright 临时测试。

---

### Task 1: 后端首页聚合接口

**Files:**
- Create: `E:\Code\QPS\QPS-HT\src\2.QPS.Application\Contracts\Dashboard\CrmDashboardDto.cs`
- Create: `E:\Code\QPS\QPS-HT\src\2.QPS.Application\Features\Dashboard\GetCrmDashboardQuery.cs`
- Create: `E:\Code\QPS\QPS-HT\src\4.QPS.WebAPI\Controllers\Admin\Dashboard\DashboardController.cs`
- Test: `E:\Code\QPS\CodexTemp\tests\QPS.UnitTests\Dashboard\CrmDashboardQueryTests.cs`

**Interfaces:**
- Consumes: `IDbContext.CrmHerbBases`, `IDbContext.CrmFollowRecords`, `IDbContext.CrmBusinessEntityAttributes`, `ICurrentUserService.UserId`
- Produces:
  - `GetCrmDashboardQuery : IRequest<CrmDashboardDto>`
  - `CrmDashboardDto`
  - `GET /api/admin/dashboard/crm`

- [ ] **Step 1: 写临时失败测试**

在 `E:\Code\QPS\CodexTemp\tests\QPS.UnitTests\Dashboard\CrmDashboardQueryTests.cs` 写测试。测试文件只保存在 `E:\Code\QPS\CodexTemp`，不复制或提交到仓库。

```csharp
using Microsoft.EntityFrameworkCore;
using QPS.Application.Interfaces;
using QPS.Application.Features.Dashboard;
using QPS.Domain.Entities.Crm;
using QPS.Infrastructure.Database;
using Xunit;

namespace QPS.UnitTests.Dashboard;

public class CrmDashboardQueryTests
{
    [Fact]
    public async Task Handle_ShouldReturnCurrentUserMetricsAndTodayFollowList()
    {
        var ownerId = Guid.Parse("11111111-1111-1111-1111-111111111111");
        var otherOwnerId = Guid.Parse("22222222-2222-2222-2222-222222222222");
        var currentUser = new FixedCurrentUserService(ownerId);
        await using var dbContext = CreateDbContext(currentUser);

        var today = DateTime.Today.AddHours(10);
        var overdue = DateTime.Now.AddDays(-1);

        var mineToday = CrmHerbBase.Create(
            "陇西黄芪基地",
            "陇西黄芪合作社",
            "HUANG_QI",
            "高",
            95,
            "甘肃省",
            "定西市",
            "陇西县",
            "首阳镇",
            null,
            null,
            "BAIDU_MAP",
            1001,
            ownerId,
            "今日跟进",
            null);
        mineToday.UpdatePrimaryContact("张经理", "13800000001");
        mineToday.UpdateFollowSummary(DateTime.Now.AddDays(-2), "INTERESTED", today);

        var mineOverdue = CrmHerbBase.Create(
            "岷县当归基地",
            "岷县当归合作社",
            "DANG_GUI",
            "中",
            82,
            "甘肃省",
            "定西市",
            "岷县",
            "梅川镇",
            null,
            null,
            "MANUAL",
            1002,
            ownerId,
            "逾期跟进",
            null);
        mineOverdue.UpdateFollowSummary(DateTime.Now.AddDays(-4), "CONNECTED", overdue);

        var otherCustomer = CrmHerbBase.Create(
            "非本人客户",
            "非本人主体",
            "TIAN_MA",
            "高",
            88,
            "云南省",
            "昭通市",
            "昭阳区",
            "测试地址",
            null,
            null,
            "BAIDU_MAP",
            1003,
            otherOwnerId,
            "不应计入我的首页",
            null);
        otherCustomer.UpdateFollowSummary(DateTime.Now, "INTERESTED", today);

        dbContext.CrmHerbBases.AddRange(mineToday, mineOverdue, otherCustomer);
        dbContext.CrmFollowRecords.Add(CrmFollowRecord.Create(
            mineToday.Id,
            null,
            "PHONE",
            "INTERESTED",
            "A",
            "客户确认下周看货",
            today.AddDays(1),
            ownerId));
        await dbContext.SaveChangesAsync();

        var result = await new GetCrmDashboardHandler(dbContext, currentUser)
            .Handle(new GetCrmDashboardQuery(), CancellationToken.None);

        Assert.Equal(1, result.Metrics.TodayFollowCount);
        Assert.Equal(1, result.Metrics.OverdueFollowCount);
        Assert.Equal(2, result.Metrics.MyCustomerCount);
        Assert.Equal(1, result.Metrics.HighIntentCustomerCount);
        Assert.Equal("岷县当归基地", result.TodayFollowCustomers[0].BaseName);
        Assert.Equal("陇西黄芪基地", result.TodayFollowCustomers[1].BaseName);
        Assert.Single(result.RecentFollowRecords);
        Assert.Contains(result.FollowFunnel, item => item.Code == "FOLLOWING");
        Assert.Contains(result.MainProductDistribution, item => item.Code == "HUANG_QI");
        Assert.Equal(7, result.FollowTrend.Count);
    }

    private static AppDbContext CreateDbContext(ICurrentUserService currentUserService)
    {
        var options = new DbContextOptionsBuilder<AppDbContext>()
            .UseInMemoryDatabase(Guid.NewGuid().ToString())
            .Options;

        return new AppDbContext(options, currentUserService);
    }

    private sealed class FixedCurrentUserService : ICurrentUserService
    {
        public FixedCurrentUserService(Guid userId)
        {
            UserId = userId.ToString();
        }

        public string? UserId { get; }
        public string? Username => "dashboard-test";
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

临时测试不能直接加入仓库项目。创建 `CodexTemp` 下的临时 xUnit 项目，引用后端源码项目，在临时项目里运行失败测试。

```powershell
$testRoot = 'E:\Code\QPS\CodexTemp\tests\QPS.DashboardTempTests'
$testFile = Join-Path $testRoot 'CrmDashboardQueryTests.cs'
if ([System.IO.Directory]::Exists($testRoot)) {
  [System.IO.Directory]::Delete($testRoot, $true)
}
dotnet new xunit -n QPS.DashboardTempTests -o $testRoot
dotnet add (Join-Path $testRoot 'QPS.DashboardTempTests.csproj') reference `
  'E:\Code\QPS\QPS-HT\src\1.QPS.Domain\QPS.Domain.csproj' `
  'E:\Code\QPS\QPS-HT\src\2.QPS.Application\QPS.Application.csproj' `
  'E:\Code\QPS\QPS-HT\src\3.QPS.Infrastructure\QPS.Infrastructure.csproj'
dotnet add (Join-Path $testRoot 'QPS.DashboardTempTests.csproj') package Microsoft.EntityFrameworkCore.InMemory --version 8.0.4
[System.IO.File]::Delete((Join-Path $testRoot 'UnitTest1.cs'))
Copy-Item -LiteralPath 'E:\Code\QPS\CodexTemp\tests\QPS.UnitTests\Dashboard\CrmDashboardQueryTests.cs' -Destination $testFile
dotnet test (Join-Path $testRoot 'QPS.DashboardTempTests.csproj') --filter FullyQualifiedName~CrmDashboardQueryTests
```

Expected: FAIL，错误包含 `QPS.Application.Features.Dashboard` 或 `GetCrmDashboardHandler` 不存在。

- [ ] **Step 3: 新增 DTO**

创建 `E:\Code\QPS\QPS-HT\src\2.QPS.Application\Contracts\Dashboard\CrmDashboardDto.cs`：

```csharp
using QPS.Application.Contracts.Crm;

namespace QPS.Application.Contracts.Dashboard;

public class CrmDashboardDto
{
    public CrmDashboardMetricsDto Metrics { get; set; } = new();
    public List<CrmDashboardFollowCustomerDto> TodayFollowCustomers { get; set; } = new();
    public List<CrmDashboardRecentFollowRecordDto> RecentFollowRecords { get; set; } = new();
    public List<CrmDashboardChartItemDto> FollowFunnel { get; set; } = new();
    public List<CrmDashboardChartItemDto> MainProductDistribution { get; set; } = new();
    public List<CrmDashboardTrendItemDto> FollowTrend { get; set; } = new();
}

public class CrmDashboardMetricsDto
{
    public int TodayFollowCount { get; set; }
    public int OverdueFollowCount { get; set; }
    public int MyCustomerCount { get; set; }
    public int HighIntentCustomerCount { get; set; }
}

public class CrmDashboardFollowCustomerDto
{
    public Guid Id { get; set; }
    public string BaseName { get; set; } = string.Empty;
    public string SubjectName { get; set; } = string.Empty;
    public string MainProduct { get; set; } = string.Empty;
    public List<string> MainProducts { get; set; } = new();
    public string Grade { get; set; } = string.Empty;
    public string Province { get; set; } = string.Empty;
    public string City { get; set; } = string.Empty;
    public string Area { get; set; } = string.Empty;
    public string PrimaryContactName { get; set; } = string.Empty;
    public string PrimaryContactPhone { get; set; } = string.Empty;
    public string LastFollowResult { get; set; } = string.Empty;
    public DateTime? NextFollowAt { get; set; }
}

public class CrmDashboardRecentFollowRecordDto
{
    public Guid Id { get; set; }
    public Guid CustomerId { get; set; }
    public string BaseName { get; set; } = string.Empty;
    public string FollowType { get; set; } = string.Empty;
    public string FollowResult { get; set; } = string.Empty;
    public string IntentLevel { get; set; } = string.Empty;
    public string Content { get; set; } = string.Empty;
    public DateTime? NextFollowAt { get; set; }
    public DateTime CreatedAt { get; set; }
}

public class CrmDashboardChartItemDto
{
    public string Code { get; set; } = string.Empty;
    public string Name { get; set; } = string.Empty;
    public int Value { get; set; }
}

public class CrmDashboardTrendItemDto
{
    public DateTime Date { get; set; }
    public int FollowCount { get; set; }
    public int EffectiveFollowCount { get; set; }
}
```

- [ ] **Step 4: 新增查询处理器**

创建 `E:\Code\QPS\QPS-HT\src\2.QPS.Application\Features\Dashboard\GetCrmDashboardQuery.cs`：

```csharp
using MediatR;
using Microsoft.EntityFrameworkCore;
using QPS.Application.Contracts.Dashboard;
using QPS.Application.Interfaces;

namespace QPS.Application.Features.Dashboard;

public class GetCrmDashboardQuery : IRequest<CrmDashboardDto>
{
}

public class GetCrmDashboardHandler : IRequestHandler<GetCrmDashboardQuery, CrmDashboardDto>
{
    private static readonly string[] ClosedStatuses = ["DEAL", "LOST", "已成交", "已流失"];
    private static readonly string[] HighGrades = ["高", "A"];
    private static readonly string[] EffectiveFollowResults = ["CONNECTED", "INTERESTED", "已接通", "有意向"];

    private readonly IDbContext _dbContext;
    private readonly ICurrentUserService _currentUserService;

    public GetCrmDashboardHandler(IDbContext dbContext, ICurrentUserService currentUserService)
    {
        _dbContext = dbContext;
        _currentUserService = currentUserService;
    }

    public async Task<CrmDashboardDto> Handle(GetCrmDashboardQuery request, CancellationToken cancellationToken)
    {
        var ownerUserId = Guid.TryParse(_currentUserService.UserId, out var parsedUserId)
            ? parsedUserId
            : (Guid?)null;

        if (!ownerUserId.HasValue)
        {
            return BuildEmptyDashboard();
        }

        var now = DateTime.Now;
        var todayStart = now.Date;
        var tomorrowStart = todayStart.AddDays(1);
        var trendStart = todayStart.AddDays(-6);
        var trendEnd = tomorrowStart;

        var myCustomers = _dbContext.CrmHerbBases
            .Where(customer => !customer.IsDeleted && customer.OwnerUserId == ownerUserId.Value);

        var activeCustomers = myCustomers.Where(customer => !ClosedStatuses.Contains(customer.Status));

        var todayFollowCount = await activeCustomers.CountAsync(
            customer => customer.NextFollowAt >= todayStart && customer.NextFollowAt < tomorrowStart,
            cancellationToken);
        var overdueFollowCount = await activeCustomers.CountAsync(
            customer => customer.NextFollowAt.HasValue && customer.NextFollowAt.Value < now,
            cancellationToken);
        var myCustomerCount = await myCustomers.CountAsync(cancellationToken);
        var highIntentCustomerCount = await activeCustomers.CountAsync(
            customer => HighGrades.Contains(customer.Grade),
            cancellationToken);

        var todayFollowCustomers = await activeCustomers
            .Where(customer =>
                customer.NextFollowAt.HasValue &&
                customer.NextFollowAt.Value < tomorrowStart)
            .OrderBy(customer => customer.NextFollowAt >= now)
            .ThenByDescending(customer => HighGrades.Contains(customer.Grade))
            .ThenBy(customer => customer.NextFollowAt)
            .Take(10)
            .Select(customer => new CrmDashboardFollowCustomerDto
            {
                Id = customer.Id,
                BaseName = customer.BaseName,
                SubjectName = customer.SubjectName,
                MainProduct = customer.MainProduct,
                Grade = customer.Grade,
                Province = customer.Province,
                City = customer.City,
                Area = customer.Area,
                PrimaryContactName = customer.PrimaryContactName,
                PrimaryContactPhone = customer.PrimaryContactPhone,
                LastFollowResult = customer.LastFollowResult,
                NextFollowAt = customer.NextFollowAt
            })
            .ToListAsync(cancellationToken);

        var customerIds = await myCustomers.Select(customer => customer.Id).ToListAsync(cancellationToken);
        var recentFollowRecords = await _dbContext.CrmFollowRecords
            .Where(record => !record.IsDeleted && customerIds.Contains(record.CustomerId))
            .OrderByDescending(record => record.CreatedAt)
            .Take(5)
            .Select(record => new CrmDashboardRecentFollowRecordDto
            {
                Id = record.Id,
                CustomerId = record.CustomerId,
                BaseName = record.Customer == null ? string.Empty : record.Customer.BaseName,
                FollowType = record.FollowType,
                FollowResult = record.FollowResult,
                IntentLevel = record.IntentLevel,
                Content = record.Content,
                NextFollowAt = record.NextFollowAt,
                CreatedAt = record.CreatedAt
            })
            .ToListAsync(cancellationToken);

        var funnelStatuses = new[]
        {
            new { Code = "PENDING", Name = "待联系" },
            new { Code = "FOLLOWING", Name = "跟进中" },
            new { Code = "INTERESTED", Name = "有意向" },
            new { Code = "DEAL", Name = "成交" },
            new { Code = "LOST", Name = "流失" }
        };
        var statusCounts = await myCustomers
            .GroupBy(customer => customer.Status)
            .Select(group => new { Status = group.Key, Count = group.Count() })
            .ToListAsync(cancellationToken);
        var interestedCount = await myCustomers.CountAsync(
            customer => customer.LastFollowResult == "INTERESTED" || customer.LastFollowResult == "有意向",
            cancellationToken);
        var followFunnel = funnelStatuses
            .Select(status => new CrmDashboardChartItemDto
            {
                Code = status.Code,
                Name = status.Name,
                Value = status.Code == "INTERESTED"
                    ? interestedCount
                    : statusCounts.FirstOrDefault(item => item.Status == status.Code)?.Count ?? 0
            })
            .ToList();

        var productAttributes = await _dbContext.CrmBusinessEntityAttributes
            .Where(attribute =>
                !attribute.IsDeleted &&
                customerIds.Contains(attribute.EntityId) &&
                attribute.EntityType == "CRM_HERB_BASE" &&
                attribute.AttributeCode == "CRM_MAIN_PRODUCT")
            .GroupBy(attribute => attribute.AttributeValue)
            .Select(group => new { Code = group.Key, Count = group.Count() })
            .ToListAsync(cancellationToken);
        var mainProductDistribution = productAttributes
            .Select(item => new CrmDashboardChartItemDto
            {
                Code = item.Code,
                Name = FormatMainProduct(item.Code),
                Value = item.Count
            })
            .OrderByDescending(item => item.Value)
            .ThenBy(item => item.Name)
            .ToList();

        var trendRecords = await _dbContext.CrmFollowRecords
            .Where(record =>
                !record.IsDeleted &&
                customerIds.Contains(record.CustomerId) &&
                record.CreatedAt >= trendStart &&
                record.CreatedAt < trendEnd)
            .Select(record => new { record.CreatedAt, record.FollowResult })
            .ToListAsync(cancellationToken);
        var followTrend = Enumerable.Range(0, 7)
            .Select(offset => trendStart.AddDays(offset))
            .Select(date =>
            {
                var nextDate = date.AddDays(1);
                var records = trendRecords
                    .Where(record => record.CreatedAt >= date && record.CreatedAt < nextDate)
                    .ToList();
                return new CrmDashboardTrendItemDto
                {
                    Date = date,
                    FollowCount = records.Count,
                    EffectiveFollowCount = records.Count(record => EffectiveFollowResults.Contains(record.FollowResult))
                };
            })
            .ToList();

        await FillMainProductsAsync(todayFollowCustomers, cancellationToken);

        return new CrmDashboardDto
        {
            Metrics = new CrmDashboardMetricsDto
            {
                TodayFollowCount = todayFollowCount,
                OverdueFollowCount = overdueFollowCount,
                MyCustomerCount = myCustomerCount,
                HighIntentCustomerCount = highIntentCustomerCount
            },
            TodayFollowCustomers = todayFollowCustomers,
            RecentFollowRecords = recentFollowRecords,
            FollowFunnel = followFunnel,
            MainProductDistribution = mainProductDistribution,
            FollowTrend = followTrend
        };
    }

    private static CrmDashboardDto BuildEmptyDashboard()
    {
        return new CrmDashboardDto
        {
            FollowFunnel =
            [
                new() { Code = "PENDING", Name = "待联系", Value = 0 },
                new() { Code = "FOLLOWING", Name = "跟进中", Value = 0 },
                new() { Code = "INTERESTED", Name = "有意向", Value = 0 },
                new() { Code = "DEAL", Name = "成交", Value = 0 },
                new() { Code = "LOST", Name = "流失", Value = 0 }
            ],
            FollowTrend = Enumerable.Range(0, 7)
                .Select(offset => DateTime.Today.AddDays(-6 + offset))
                .Select(date => new CrmDashboardTrendItemDto { Date = date })
                .ToList()
        };
    }

    private async Task FillMainProductsAsync(
        List<CrmDashboardFollowCustomerDto> customers,
        CancellationToken cancellationToken)
    {
        var customerIds = customers.Select(customer => customer.Id).ToList();
        if (customerIds.Count == 0)
        {
            return;
        }

        var attributes = await _dbContext.CrmBusinessEntityAttributes
            .Where(attribute =>
                !attribute.IsDeleted &&
                customerIds.Contains(attribute.EntityId) &&
                attribute.EntityType == "CRM_HERB_BASE" &&
                attribute.AttributeCode == "CRM_MAIN_PRODUCT")
            .OrderBy(attribute => attribute.SortOrder)
            .Select(attribute => new { attribute.EntityId, attribute.AttributeValue })
            .ToListAsync(cancellationToken);

        var lookup = attributes
            .GroupBy(attribute => attribute.EntityId)
            .ToDictionary(
                group => group.Key,
                group => group.Select(attribute => attribute.AttributeValue).Distinct().ToList());

        foreach (var customer in customers)
        {
            if (lookup.TryGetValue(customer.Id, out var mainProducts))
            {
                customer.MainProducts = mainProducts;
            }
        }
    }

    private static string FormatMainProduct(string code)
    {
        return code switch
        {
            "HUANG_QI" => "黄芪",
            "DANG_GUI" => "当归",
            "DANG_SHEN" => "党参",
            "TIAN_MA" => "天麻",
            "OTHER" => "其他",
            _ => code
        };
    }
}
```

- [ ] **Step 5: 新增控制器**

创建 `E:\Code\QPS\QPS-HT\src\4.QPS.WebAPI\Controllers\Admin\Dashboard\DashboardController.cs`：

```csharp
using MediatR;
using Microsoft.AspNetCore.Mvc;
using QPS.Application.Contracts.Dashboard;
using QPS.Application.Features.Dashboard;

namespace QPS.WebAPI.Controllers.Admin.Dashboard;

[Route("api/admin/dashboard")]
[ApiController]
public class DashboardController : ControllerBase
{
    private readonly IMediator _mediator;

    public DashboardController(IMediator mediator)
    {
        _mediator = mediator;
    }

    [HttpGet("crm")]
    public async Task<ActionResult<CrmDashboardDto>> GetCrmDashboard()
    {
        var result = await _mediator.Send(new GetCrmDashboardQuery());
        return Ok(result);
    }
}
```

- [ ] **Step 6: 跑后端验证**

```powershell
dotnet test 'E:\Code\QPS\CodexTemp\tests\QPS.DashboardTempTests\QPS.DashboardTempTests.csproj' --filter FullyQualifiedName~CrmDashboardQueryTests
dotnet test 'E:\Code\QPS\QPS-HT\QPS.sln'
```

Expected: 临时 dashboard 测试通过，`QPS.UnitTests` 通过；`QPS.IntegrationTests` 如果仍无可发现测试，记录为既有状态。

- [ ] **Step 7: 提交后端聚合接口**

```powershell
git -C 'E:\Code\QPS\QPS-HT' status --short
git -C 'E:\Code\QPS\QPS-HT' add -- `
  'src/2.QPS.Application/Contracts/Dashboard/CrmDashboardDto.cs' `
  'src/2.QPS.Application/Features/Dashboard/GetCrmDashboardQuery.cs' `
  'src/4.QPS.WebAPI/Controllers/Admin/Dashboard/DashboardController.cs'
git -C 'E:\Code\QPS\QPS-HT' commit -m 'feat: 新增 CRM 首页工作台聚合接口'
```

---

### Task 2: 前端 Dashboard API 和类型

**Files:**
- Create: `E:\Code\QPS\QPS_WEB_ADMIN\src\api\modules\dashboard.ts`
- Test: `E:\Code\QPS\CodexTemp\tests\QPS_WEB_ADMIN\home-dashboard-api.spec.ts`

**Interfaces:**
- Consumes: `GET /api/admin/dashboard/crm`
- Produces:
  - `dashboardApi.getCrmDashboard(): Promise<ResultData<CrmDashboardData>>`
  - `CrmDashboardData`

- [ ] **Step 1: 写临时 Playwright API mock 测试**

创建 `E:\Code\QPS\CodexTemp\tests\QPS_WEB_ADMIN\home-dashboard-api.spec.ts`：

```ts
import { expect, test } from "@playwright/test";

test("dashboard api contract returns crm dashboard sections", async ({ request }) => {
  const body = {
    code: 200,
    msg: "success",
    data: {
      metrics: {
        todayFollowCount: 2,
        overdueFollowCount: 1,
        myCustomerCount: 9,
        highIntentCustomerCount: 3
      },
      todayFollowCustomers: [],
      recentFollowRecords: [],
      followFunnel: [{ code: "FOLLOWING", name: "跟进中", value: 5 }],
      mainProductDistribution: [{ code: "HUANG_QI", name: "黄芪", value: 4 }],
      followTrend: [{ date: "2026-07-31T00:00:00", followCount: 2, effectiveFollowCount: 1 }]
    }
  };

  expect(body.data.metrics.todayFollowCount).toBe(2);
  expect(body.data.followFunnel[0].name).toBe("跟进中");
  expect(body.data.mainProductDistribution[0].code).toBe("HUANG_QI");
});
```

- [ ] **Step 2: 创建 API 模块**

创建 `E:\Code\QPS\QPS_WEB_ADMIN\src\api\modules\dashboard.ts`：

```ts
import http from "@/api";

export interface CrmDashboardMetrics {
  todayFollowCount: number;
  overdueFollowCount: number;
  myCustomerCount: number;
  highIntentCustomerCount: number;
}

export interface CrmDashboardFollowCustomer {
  id: string;
  baseName: string;
  subjectName: string;
  mainProduct: string;
  mainProducts: string[];
  grade: string;
  province: string;
  city: string;
  area: string;
  primaryContactName: string;
  primaryContactPhone: string;
  lastFollowResult: string;
  nextFollowAt?: string | null;
}

export interface CrmDashboardRecentFollowRecord {
  id: string;
  customerId: string;
  baseName: string;
  followType: string;
  followResult: string;
  intentLevel: string;
  content: string;
  nextFollowAt?: string | null;
  createdAt: string;
}

export interface CrmDashboardChartItem {
  code: string;
  name: string;
  value: number;
}

export interface CrmDashboardTrendItem {
  date: string;
  followCount: number;
  effectiveFollowCount: number;
}

export interface CrmDashboardData {
  metrics: CrmDashboardMetrics;
  todayFollowCustomers: CrmDashboardFollowCustomer[];
  recentFollowRecords: CrmDashboardRecentFollowRecord[];
  followFunnel: CrmDashboardChartItem[];
  mainProductDistribution: CrmDashboardChartItem[];
  followTrend: CrmDashboardTrendItem[];
}

export const dashboardApi = {
  getCrmDashboard: () => {
    return http.get<CrmDashboardData>("/admin/dashboard/crm", undefined, { loading: false });
  }
};

export default dashboardApi;
```

- [ ] **Step 3: 跑前端构建验证类型**

```powershell
npm.cmd run build
```

Expected: build 通过；如产生 `dist`，任务结束前删除。

- [ ] **Step 4: 提交前端 API**

```powershell
git -C 'E:\Code\QPS\QPS_WEB_ADMIN' add -- 'src/api/modules/dashboard.ts'
git -C 'E:\Code\QPS\QPS_WEB_ADMIN' commit -m 'feat: 新增首页工作台接口模块'
```

---

### Task 3: 首页组件和页面

**Files:**
- Create: `E:\Code\QPS\QPS_WEB_ADMIN\src\views\home\components\HomeMetricCards.vue`
- Create: `E:\Code\QPS\QPS_WEB_ADMIN\src\views\home\components\TodayFollowTable.vue`
- Create: `E:\Code\QPS\QPS_WEB_ADMIN\src\views\home\components\HomeQuickActions.vue`
- Create: `E:\Code\QPS\QPS_WEB_ADMIN\src\views\home\components\RecentFollowRecords.vue`
- Create: `E:\Code\QPS\QPS_WEB_ADMIN\src\views\home\components\FollowFunnelChart.vue`
- Create: `E:\Code\QPS\QPS_WEB_ADMIN\src\views\home\components\MainProductDistributionChart.vue`
- Create: `E:\Code\QPS\QPS_WEB_ADMIN\src\views\home\components\FollowTrendChart.vue`
- Modify: `E:\Code\QPS\QPS_WEB_ADMIN\src\views\home\index.vue`
- Test: `E:\Code\QPS\CodexTemp\tests\QPS_WEB_ADMIN\home-dashboard.spec.ts`

**Interfaces:**
- Consumes: `dashboardApi.getCrmDashboard()`
- Produces: 可操作首页工作台，展示 KPI、待跟进列表、快捷入口、最近沟通、3 个图表

- [ ] **Step 1: 写临时 Playwright 首页测试**

创建 `E:\Code\QPS\CodexTemp\tests\QPS_WEB_ADMIN\home-dashboard.spec.ts`：

```ts
import { expect, test } from "@playwright/test";

test.beforeEach(async ({ page }) => {
  await page.route("**/api/admin/auth/login", async route => {
    await route.fulfill({
      status: 200,
      contentType: "application/json",
      body: JSON.stringify({
        code: 200,
        msg: "success",
        data: {
          token: "playwright-token",
          userId: "11111111-1111-1111-1111-111111111111",
          username: "admin",
          realName: "测试管理员",
          role: "admin"
        }
      })
    });
  });

  await page.route("**/api/admin/auth/user-permissions", async route => {
    await route.fulfill({
      status: 200,
      contentType: "application/json",
      body: JSON.stringify({
        code: 200,
        msg: "success",
        data: {
          permissions: ["HOME", "CRM", "CRM_HERB_BASE", "CRM_VENDOR", "DATA_DICTIONARY"],
          buttons: []
        }
      })
    });
  });

  await page.route("**/api/admin/dashboard/crm", async route => {
    await route.fulfill({
      status: 200,
      contentType: "application/json",
      body: JSON.stringify({
        code: 200,
        msg: "success",
        data: {
          metrics: {
            todayFollowCount: 2,
            overdueFollowCount: 1,
            myCustomerCount: 12,
            highIntentCustomerCount: 4
          },
          todayFollowCustomers: [
            {
              id: "aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa",
              baseName: "陇西黄芪基地",
              subjectName: "陇西黄芪合作社",
              mainProduct: "HUANG_QI",
              mainProducts: ["HUANG_QI"],
              grade: "高",
              province: "甘肃省",
              city: "定西市",
              area: "陇西县",
              primaryContactName: "张经理",
              primaryContactPhone: "13800000001",
              lastFollowResult: "INTERESTED",
              nextFollowAt: "2026-07-31T10:00:00"
            }
          ],
          recentFollowRecords: [
            {
              id: "bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbbbbbb",
              customerId: "aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa",
              baseName: "陇西黄芪基地",
              followType: "PHONE",
              followResult: "INTERESTED",
              intentLevel: "A",
              content: "客户确认下周看货",
              nextFollowAt: "2026-08-01T10:00:00",
              createdAt: "2026-07-31T09:00:00"
            }
          ],
          followFunnel: [
            { code: "PENDING", name: "待联系", value: 3 },
            { code: "FOLLOWING", name: "跟进中", value: 5 },
            { code: "INTERESTED", name: "有意向", value: 4 },
            { code: "DEAL", name: "成交", value: 1 },
            { code: "LOST", name: "流失", value: 1 }
          ],
          mainProductDistribution: [
            { code: "HUANG_QI", name: "黄芪", value: 6 },
            { code: "DANG_GUI", name: "当归", value: 3 }
          ],
          followTrend: [
            { date: "2026-07-25T00:00:00", followCount: 1, effectiveFollowCount: 1 },
            { date: "2026-07-26T00:00:00", followCount: 0, effectiveFollowCount: 0 },
            { date: "2026-07-27T00:00:00", followCount: 2, effectiveFollowCount: 1 },
            { date: "2026-07-28T00:00:00", followCount: 3, effectiveFollowCount: 2 },
            { date: "2026-07-29T00:00:00", followCount: 1, effectiveFollowCount: 0 },
            { date: "2026-07-30T00:00:00", followCount: 4, effectiveFollowCount: 3 },
            { date: "2026-07-31T00:00:00", followCount: 2, effectiveFollowCount: 1 }
          ]
        }
      })
    });
  });

  await page.goto("/login");
  await page.getByRole("button", { name: /登录/ }).click();
});

test("home dashboard renders crm workbench sections", async ({ page }) => {
  await page.goto("/#/home");

  await expect(page.getByRole("heading", { name: "我的 CRM 工作台" })).toBeVisible();
  await expect(page.getByText("今日待跟进")).toBeVisible();
  await expect(page.getByText("逾期未跟进")).toBeVisible();
  await expect(page.getByText("我的客户")).toBeVisible();
  await expect(page.getByText("高意向客户")).toBeVisible();
  await expect(page.getByText("陇西黄芪基地")).toBeVisible();
  await expect(page.getByText("客户确认下周看货")).toBeVisible();
  await expect(page.getByText("跟进漏斗")).toBeVisible();
  await expect(page.getByText("主营品类分布")).toBeVisible();
  await expect(page.getByText("近 7 天跟进趋势")).toBeVisible();
});
```

- [ ] **Step 2: 运行测试确认失败**

```powershell
npm.cmd run test:e2e -- E:\Code\QPS\CodexTemp\tests\QPS_WEB_ADMIN\home-dashboard.spec.ts
```

Expected: FAIL，首页标题或 dashboard 接口模块不存在。

- [ ] **Step 3: 实现展示组件**

组件要求：

- `HomeMetricCards.vue` 使用 4 个指标项，props 为 `metrics`，emit `metric-click`。
- `TodayFollowTable.vue` 使用 `el-table`，props 为 `customers`，emit `open-detail` 和 `record-follow`。
- `HomeQuickActions.vue` 使用 `el-button` 和 lucide 不可用时使用 Element Plus icons，emit `go`。
- `RecentFollowRecords.vue` 使用列表展示最近 5 条沟通，空态用 `el-empty`。
- 3 个图表组件使用 `vue-echarts` 的 `VChart`，固定高度 260px，空数据用 `el-empty`。

`FollowFunnelChart.vue` 图表 option 核心：

```ts
const option = computed(() => ({
  tooltip: { trigger: "item" },
  series: [
    {
      type: "funnel",
      left: "8%",
      right: "8%",
      top: 16,
      bottom: 16,
      minSize: "20%",
      maxSize: "90%",
      sort: "none",
      label: { formatter: "{b} {c}" },
      data: props.items.map(item => ({ name: item.name, value: item.value }))
    }
  ]
}));
```

`MainProductDistributionChart.vue` 图表 option 核心：

```ts
const option = computed(() => ({
  tooltip: { trigger: "item" },
  legend: { bottom: 0, type: "scroll" },
  series: [
    {
      type: "pie",
      radius: ["48%", "70%"],
      center: ["50%", "45%"],
      label: { formatter: "{b} {c}" },
      data: props.items.map(item => ({ name: item.name, value: item.value }))
    }
  ]
}));
```

`FollowTrendChart.vue` 图表 option 核心：

```ts
const option = computed(() => ({
  tooltip: { trigger: "axis" },
  grid: { left: 36, right: 16, top: 24, bottom: 32 },
  xAxis: {
    type: "category",
    data: props.items.map(item => new Date(item.date).toLocaleDateString("zh-CN", { month: "2-digit", day: "2-digit" }))
  },
  yAxis: { type: "value", minInterval: 1 },
  series: [
    { name: "沟通次数", type: "line", smooth: true, data: props.items.map(item => item.followCount) },
    { name: "有效沟通", type: "line", smooth: true, data: props.items.map(item => item.effectiveFollowCount) }
  ]
}));
```

- [ ] **Step 4: 改造首页页面**

修改 `E:\Code\QPS\QPS_WEB_ADMIN\src\views\home\index.vue`：

```ts
import { computed, onMounted, ref } from "vue";
import { useRouter } from "vue-router";
import { ElMessage } from "element-plus";
import dashboardApi, { type CrmDashboardData } from "@/api/modules/dashboard";
import HomeMetricCards from "./components/HomeMetricCards.vue";
import TodayFollowTable from "./components/TodayFollowTable.vue";
import HomeQuickActions from "./components/HomeQuickActions.vue";
import RecentFollowRecords from "./components/RecentFollowRecords.vue";
import FollowFunnelChart from "./components/FollowFunnelChart.vue";
import MainProductDistributionChart from "./components/MainProductDistributionChart.vue";
import FollowTrendChart from "./components/FollowTrendChart.vue";

const router = useRouter();
const loading = ref(false);
const errorMessage = ref("");
const dashboard = ref<CrmDashboardData>({
  metrics: {
    todayFollowCount: 0,
    overdueFollowCount: 0,
    myCustomerCount: 0,
    highIntentCustomerCount: 0
  },
  todayFollowCustomers: [],
  recentFollowRecords: [],
  followFunnel: [],
  mainProductDistribution: [],
  followTrend: []
});

const loadDashboard = async () => {
  loading.value = true;
  errorMessage.value = "";
  try {
    const result = await dashboardApi.getCrmDashboard();
    dashboard.value = result.data;
  } catch (error) {
    errorMessage.value = "首页数据加载失败";
    ElMessage.error(errorMessage.value);
  } finally {
    loading.value = false;
  }
};

const goHerbBaseList = (query: Record<string, string | boolean> = {}) => {
  router.push({ path: "/crm/herb-base", query });
};

const handleMetricClick = (type: string) => {
  if (type === "today") {
    goHerbBaseList({ followFilter: "today" });
  } else if (type === "overdue") {
    goHerbBaseList({ onlyOverdue: true });
  } else if (type === "highIntent") {
    goHerbBaseList({ grade: "高" });
  } else {
    goHerbBaseList();
  }
};

onMounted(loadDashboard);
```

模板布局要求：

- 页面根节点 class：`home-dashboard`
- 标题：`我的 CRM 工作台`
- 顶部：`HomeMetricCards`
- 主区：左侧 `TodayFollowTable`，右侧 `HomeQuickActions` + `RecentFollowRecords`
- 下方：三个图表组件
- 错误态：显示 `el-alert` 和“重试”按钮

- [ ] **Step 5: 跑前端测试和构建**

```powershell
npm.cmd run test:e2e -- E:\Code\QPS\CodexTemp\tests\QPS_WEB_ADMIN\home-dashboard.spec.ts
npm.cmd run build
```

Expected: Playwright 首页测试通过，构建通过。

- [ ] **Step 6: 提交前端首页**

```powershell
git -C 'E:\Code\QPS\QPS_WEB_ADMIN' add -- `
  'src/views/home/index.vue' `
  'src/views/home/components/HomeMetricCards.vue' `
  'src/views/home/components/TodayFollowTable.vue' `
  'src/views/home/components/HomeQuickActions.vue' `
  'src/views/home/components/RecentFollowRecords.vue' `
  'src/views/home/components/FollowFunnelChart.vue' `
  'src/views/home/components/MainProductDistributionChart.vue' `
  'src/views/home/components/FollowTrendChart.vue'
git -C 'E:\Code\QPS\QPS_WEB_ADMIN' commit -m 'feat: 实现 CRM 首页工作台'
```

---

### Task 4: 端到端验证和产物清理

**Files:**
- Modify: none
- Test: use existing commands and temporary files in `E:\Code\QPS\CodexTemp`

**Interfaces:**
- Consumes: Task 1、Task 2、Task 3 全部产物
- Produces: 可运行、可验证的首页工作台

- [ ] **Step 1: 跑后端全量测试**

```powershell
dotnet test 'E:\Code\QPS\QPS-HT\QPS.sln'
```

Expected: 单测通过；如集成测试项目仍显示没有可用测试，记录为既有状态。

- [ ] **Step 2: 跑前端全量 e2e**

```powershell
npm.cmd run test:e2e
```

Expected: 既有 e2e 全部通过。

- [ ] **Step 3: 跑前端构建**

```powershell
npm.cmd run build
```

Expected: 构建通过。Vite chunk size warning 可以记录为既有警告，不作为失败。

- [ ] **Step 4: 启动后端验证接口**

```powershell
dotnet run --urls http://localhost:5000
```

工作目录：`E:\Code\QPS\QPS-HT\src\4.QPS.WebAPI`

Expected: 日志出现 `Now listening on: http://localhost:5000`。

- [ ] **Step 5: 调接口 smoke**

```powershell
$login = Invoke-RestMethod -Method Post -Uri 'http://localhost:5000/api/admin/auth/login' -ContentType 'application/json' -Body '{"username":"admin","password":"123456"}'
$token = $login.data.token
Invoke-RestMethod -Method Get -Uri 'http://localhost:5000/api/admin/dashboard/crm' -Headers @{ Authorization = "Bearer $token" }
```

Expected: 返回包含 `metrics`、`todayFollowCustomers`、`recentFollowRecords`、`followFunnel`、`mainProductDistribution`、`followTrend`。

- [ ] **Step 6: 清理中间产物**

```powershell
$targets = @(
  'E:\Code\QPS\CodexTemp\tests\QPS.DashboardTempTests',
  'E:\Code\QPS\QPS_WEB_ADMIN\dist',
  'E:\Code\QPS\QPS_WEB_ADMIN\playwright-report',
  'E:\Code\QPS\QPS_WEB_ADMIN\test-results'
)
$allowedRoots = @(
  [System.IO.Path]::GetFullPath('E:\Code\QPS\CodexTemp\'),
  [System.IO.Path]::GetFullPath('E:\Code\QPS\QPS_WEB_ADMIN\')
)
foreach ($target in $targets) {
  $full = [System.IO.Path]::GetFullPath($target)
  $isAllowed = $allowedRoots | Where-Object {
    $full.StartsWith($_, [System.StringComparison]::OrdinalIgnoreCase)
  }
  if (-not $isAllowed) {
    throw "Refusing to delete outside workspace: $full"
  }
  if ([System.IO.Directory]::Exists($full)) {
    [System.IO.Directory]::Delete($full, $true)
  }
}
```

- [ ] **Step 7: 确认临时测试文件不在仓库**

```powershell
git -C 'E:\Code\QPS\QPS_WEB_ADMIN' status --short -- 'e2e' 'test-results' 'playwright-report' 'dist'
git -C 'E:\Code\QPS\QPS-HT' status --short -- 'tests/QPS.UnitTests/Dashboard'
```

Expected: 没有新增 dashboard 测试文件进入仓库；构建和测试产物不存在。

- [ ] **Step 8: 最终检查字段和文档**

```powershell
rg -n "SourceLeadId|sourceLeadId" -S 'E:\Code\QPS\QPS-HT\src' 'E:\Code\QPS\QPS_WEB_ADMIN\src'
Get-ChildItem -LiteralPath 'E:\Code\QPS\QPS_WEB_ADMIN\docs\superpowers\plans' | Select-Object Name
```

Expected: `SourceLeadId` 只允许出现在旧数据库兼容 SQL 中；计划文档名为中文。

---

## Self-Review

- Spec coverage: 当前用户默认范围、KPI、今日待跟进、快捷操作、最近沟通、跟进漏斗、主营品类分布、近 7 天趋势、错误态、空态和验证命令都已映射到任务。
- Placeholder scan: 文档不包含 TBD、TODO 或“以后补”等占位要求。
- Type consistency: 后端返回 `CrmDashboardDto`，前端类型为 `CrmDashboardData`，字段采用 camelCase，与现有 Axios 响应处理一致。
- Test placement: 后端临时测试通过 `CodexTemp` 下的临时 xUnit 项目执行，不向后端测试目录复制文件；前端 Playwright 临时测试也只保存在 `CodexTemp`。
- Scope check: 计划聚焦首页工作台，不包含管理视角切换、大屏、地图或排行榜。
